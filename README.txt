
image-r3 example configuration
--------------------------

To run this example configuration, use 

  java -jar start.jar

in this directory, and when image-r3 is started connect to 

  http://localhost:80
